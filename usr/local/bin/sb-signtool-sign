#!/usr/bin/env bash

source /usr/local/etc/sb-signtool/mok.rc

KERNEL=$(uname -r)
KDIR=/lib/modules/"$KERNEL"

while read in
do
  $KDIR/build/scripts/sign-file sha256 $PRIVATE_KEY_FILE $PUBLIC_KEY_FILE $KDIR/$in
done < /usr/local/etc/sb-signtool/modules.conf
